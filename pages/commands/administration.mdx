import Callout from "nextra-theme-docs/callout";
import Command from "../../components/features.js"

# Administration
Administration commands can only be used by users with administrative privileges.

<Callout emoji="ℹ️" background = 'bg-blue-100 dark:text-gray-800'>
    You should replace `£` with the current prefix you have set.
    More information on how to change the prefix here.
</Callout>

## addrole
Lets you assign a role to a member.

<small>This command can only be executed on a server.</small>

```
£addrole [ member / member ID ] [ role / role ID ]

How to use the command
£addrole @Asgarrrr @Admin
£addrole @Twiistrz @Moderator
```
<small style={{ position: 'relative', top: '-15px' }}>`[]` = Required arguments, — `{}` = Optional arguments.</small>

## autorole
Defines the roles assigned automatically when a new member joins.

```
£autorole [ operation ] [ role ]

How to use the command
£autorole on
£autorole off
£autorole add @Verified
£autorole remove @Admin
£autorole view
```
<small style={{ position: 'relative', top: '-15px' }}>`[]` = Required arguments, — `{}` = Optional arguments.</small>

## language
Change the language used by Luna in the guild.

```
£language { language }

How to use the command
£language
£language FR
£language EN
```
<small style={{ position: 'relative', top: '-15px' }}>`[]` = Required arguments, — `{}` = Optional arguments.</small>

## prefix
Change the prefix used by Luna in the guild.

```
£prefix [ prefix ]

How to use the command
£prefix _
£prefix !
```
<small style={{ position: 'relative', top: '-15px' }}>`[]` = Required arguments, — `{}` = Optional arguments.</small>